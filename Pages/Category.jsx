import { Box } from "@chakra-ui/react";
import React from "react";

const Category = ({ data }) => {
    return (
        <Box mt={["40px", null, "100px"]}>
            <Box>
                
            </Box>
        </Box>
    );
};

export default Category;
